# Temporary files
*.swp
*.swo
*.pyc
.DS_Store

# IDE
.vscode/
.idea/

# Private context
private/

# Environment variables and Python cache (ui/)
ui/.env
ui/__pycache__/
ui/.venv/
ui/venv/
ui/logs/
ui/.discord_model

# Memory files (local-only, not synced to repo)
memory/

# Scratchpad for local working files
scratchpad/

# Project workspace (local development only, not synced to repo)
idea/

# Email system credentials and local config (contains API keys, credentials, etc.)
*.email.config.json
lucent_email.config.json
.lucent/
src/lucent_email/*.db
src/lucent_email/*.db-*
ui/.auth/
scratch pad/

# PATH deck — build/exercise output, ~107MB of it video (per Nick 2026-08-06).
# The real PATH presentation gets rebuilt later; AIVU/chessloop stay tracked.
presentation/PATH/
